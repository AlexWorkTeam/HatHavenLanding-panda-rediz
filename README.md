# HatHaven Landing - Legal Refund Quiz

Лендинг-пейдж с интерактивным квизом для юридической службы по возврату денег жертвам финансового мошенничества.

## 📋 Описание проекта

Проект представляет собой современный одностраничный сайт (SPA) с квизом из 6 вопросов, который помогает оценить шансы на возврат потерянных средств. После прохождения квиза пользователь заполняет форму заявки, данные сохраняются в базе данных и отправляются через webhook в Make.com.

### Основные возможности

- **Главная страница** с hero-секцией и призывом к действию
- **Exit-intent popup** при попытке покинуть сайт
- **6-шаговый квиз** с вопросами о мошенничестве
- **Экран анализа** с расчетом вероятности возврата средств
- **Форма заявки** для сбора контактных данных
- **Страницы** Privacy Policy, Terms of Use, Disclaimer
- **Интеграция с Make.com** через webhook для обработки лидов

## 🏗️ Архитектура проекта

Проект использует монорепозиторий с разделением на клиентскую и серверную части:

```
├── client/          # React фронтенд приложение
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы приложения
│   │   ├── hooks/       # React хуки
│   │   └── lib/         # Утилиты и конфигурация
│   └── public/         # Статические файлы
├── server/          # Express.js бэкенд
│   ├── app.ts       # Основной файл Express приложения
│   ├── routes.ts    # API маршруты
│   └── storage.ts   # Слой работы с данными
├── shared/          # Общие типы и схемы
│   └── schema.ts    # Zod схемы и Drizzle ORM модели
└── attached_assets/ # Изображения и медиа файлы
```

## 🛠️ Технологический стек

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Wouter** - роутинг (легковесная альтернатива React Router)
- **TanStack Query** - управление серверным состоянием
- **Tailwind CSS** - стилизация
- **Radix UI** - компоненты UI (47 компонентов)
- **Framer Motion** - анимации
- **React Hook Form** - управление формами
- **Zod** - валидация данных
- **Vite** - сборщик и dev-сервер

### Backend
- **Express.js** - веб-сервер
- **Drizzle ORM** - работа с базой данных
- **PostgreSQL** (Neon) - база данных
- **Express Session** - управление сессиями
- **Passport.js** - аутентификация (настроено, но не используется)

### Инструменты разработки
- **TypeScript** - типизация
- **ESBuild** - сборка продакшн версии
- **Drizzle Kit** - миграции БД
- **TSX** - запуск TypeScript файлов

## 📦 Установка и запуск

### Требования
- Node.js 20+
- PostgreSQL база данных (или Neon)
- Переменные окружения (см. ниже)

### Установка зависимостей

```bash
npm install
```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL=postgresql://user:password@host:port/database
PORT=5000
NODE_ENV=development
```

### Разработка

Запуск dev-сервера (клиент + сервер):

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5000`

### Сборка для продакшна

```bash
npm run build
```

### Запуск продакшн версии

```bash
npm start
```

### Миграции базы данных

```bash
npm run db:push
```

## 📁 Структура компонентов

### Основные страницы

- **Landing** (`/`) - главная страница с hero-секцией
- **Quiz** (`/quiz`) - квиз из 6 вопросов
- **ThankYou** (`/thank-you`) - страница благодарности после отправки формы
- **PrivacyPolicy** (`/privacy-policy`) - политика конфиденциальности
- **TermsOfUse** (`/terms-of-use`) - условия использования
- **Disclaimer** (`/disclaimer`) - отказ от ответственности

### Компоненты квиза

- **QuizStep** - шаг квиза с вопросом и опциями
- **QuizOption** - опция ответа в квизе
- **QuizProgress** - индикатор прогресса
- **AnalyzingScreen** - экран анализа с анимацией
- **LeadForm** - форма сбора контактных данных

### UI компоненты

Проект использует shadcn/ui компоненты (47 компонентов на базе Radix UI):
- Button, Input, Card, Dialog, Toast, Tooltip и др.

## 🎨 Дизайн

### Цветовая схема
- **Primary**: `#0A2540` (глубокий сапфировый)
- **Accent**: `#00D4FF` (яркий циан)
- **Secondary**: `#6E56CF` (фиолетовый)
- **Background**: `#F7FAFC`
- **Success**: `#10B981` (зеленый)

### Типографика
- **Заголовки**: Inter Bold (700-800), 32-48px
- **Подзаголовки**: Inter SemiBold (600), 20-24px
- **Основной текст**: Inter Regular (400), 16-18px
- **Кнопки**: Manrope Bold (700), 16px, uppercase

### Стиль
- Glassmorphism эффекты на карточках
- Градиенты на кнопках: `linear-gradient(135deg, #00D4FF 0%, #6E56CF 100%)`
- Плавные анимации (0.3s ease)
- Hover эффекты с масштабированием (scale 1.02)

## 🔄 Бизнес-логика

### Квиз вопросы

1. **Тип компании-мошенника** (Инвестиционная/Букмекер/Казино/Форекс/Крипто)
2. **Дата мошенничества** (менее 3 месяцев / 3-6 месяцев / 6-12 месяцев / 1-3 года / более 3 лет)
3. **Статус денег** (Заморожены / Счёт обнулён / Нет связи / Нет доступа / Не знаю)
4. **Сумма потерь** (До $5K / $5K-$25K / $25K-$100K / $100K-$500K / Более $500K)
5. **Способ оплаты** (Карта / Wire Transfer / ACH / Крипто / Платежные приложения / Чек / Наличные)
6. **Документация** (Оригиналы / Электронные копии / Только переписка / Нет документов)

### Расчет вероятности возврата

Алгоритм расчета базируется на ответах пользователя:
- Базовая вероятность: 75%
- Тип компании: +4% до +8%
- Временной фактор: +2% до +10%
- Документация: +2% до +7%
- Способ оплаты: +1% до +3%
- Максимальная вероятность: 95%

### Обработка лидов

При отправке формы:
1. Валидация данных через Zod схему
2. Сохранение в памяти (MemStorage)
3. Запись в `leads_log.txt`
4. Отправка webhook в Make.com (асинхронно, без блокировки)

## 🔌 API Endpoints

### POST `/api/leads`

Создание нового лида.

**Request Body:**
```typescript
{
  first_name: string;
  last_name: string;
  phone: string; // формат: 10 цифр США
  email: string;
  companyType: string;
  fraudDate: string;
  moneyStatus: string;
  amount: string;
  paymentMethod: string;
  documentation: string;
  dataConsent: boolean; // должно быть true
  ageConsent: boolean; // должно быть true
}
```

**Response:**
```typescript
{
  success: true;
  lead: Lead & { id: string; createdAt: Date };
}
```

## 📊 База данных

### Схема (Drizzle ORM)

**users** - таблица пользователей (для будущей аутентификации)
- `id` (varchar, UUID)
- `username` (text, unique)
- `password` (text)

**leads** - хранятся в памяти (MemStorage), не в БД
- Все поля из `leadSchema`
- `id` (UUID)
- `createdAt` (Date)

## 🔗 Интеграции

### Make.com Webhook

URL: `https://hook.eu2.make.com/mjdxoefdh8c4itgjpirwchafiyhaqixk`

**Payload:**
```json
{
  "email": "string",
  "full_name": "string",
  "landing": "legal-refund.replit.app",
  "country": "US",
  "landing_name": "legal-refund-us-ru",
  "user_id": 3,
  "source": "Google",
  "phone": "string",
  "ip": "string",
  "description": "Company Type: ..., Fraud Date: ..., ..."
}
```

## 📝 Логирование

Все лиды логируются в файл `leads_log.txt` в формате:

```
[2025-01-24T12:00:00.000Z]
Name: Иван Иванов
Phone: 1234567890
Email: example@email.com
Quiz Answers:
  - Company Type: Криптобиржа или кошелёк
  - Fraud Date: Менее 3 месяцев назад
  - Money Status: Заморожены на счету
  - Amount: $5,000 - $25,000
  - Payment Method: Кредитная/дебетовая карта
  - Documentation: Да, есть оригиналы
-------------------
```

## 🚀 Деплой

Проект настроен для деплоя на Replit:
- Использует плагины Replit для разработки
- Порт задается через переменную окружения `PORT` (по умолчанию 5000)
- Продакшн сборка через `npm run build`
- Статические файлы клиента собираются в `dist/public`

## 📄 Лицензия

MIT

## 👥 Авторы

Проект разработан для юридической службы по возврату денег жертвам финансового мошенничества.

